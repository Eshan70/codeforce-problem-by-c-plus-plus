#include <bits/stdc++.h>
 
using namespace std;
 
int main() {
	
 
	int tt; 
	cin >> tt;
	while (tt--) {
		
 
		long long n, k; cin >> n >> k;
 
		if (k % 2 == 0 && n % 2 == 1)
			cout << "NO"<<endl;
		else
			cout << "YES"<<endl;;
	}
}
