#include<iostream>
using namespace std;
int main()
{
    int a;

    cin>>a;

    for(int i=0; i<a; i++)
    {
        int a,b;

        cin>>a>>b;
        if(a%b==0)
        {
            cout<<"0"<<endl;
        }
        else
        {
            cout<<b-(a%b)<<endl;
        }
    }
}
