#include <iostream>
using namespace std;
 
int main() {
	int k2, k3, k5, k6;
	cin >> k2 >> k3 >> k5 >> k6;
    int count = min(k2, min(k5, k6));
    int count1 = min((k2 - count), k3);
    cout << 256 * count + 32 * count1 << endl;
 
}
